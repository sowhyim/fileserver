# fileserver-backend (Deliberately not using third-party libraries while creating http-server)

## Upload
    - Fragment upload mode
    - first： create upload task; then: upload anything with md5; last: merge.